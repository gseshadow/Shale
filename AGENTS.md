# Codex Repository Bootstrap

Before planning or editing, read `architecture/codex-prompt-rules.md` completely and follow its routing instructions to the relevant files in `architecture/` and `docs/`.

Inspect related existing tests before changing production code, and maintain those tests as part of the implementation rather than waiting for failures. In the first progress update, report which architecture documents were reviewed.

Do not declare completion until the relevant tests have passed. If a required test is blocked or unexecuted, report the work as unverified rather than claiming success.
